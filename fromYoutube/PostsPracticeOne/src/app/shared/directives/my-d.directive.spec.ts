import { MyDDirective } from './my-d.directive';

describe('MyDDirective', () => {
  it('should create an instance', () => {
    const directive = new MyDDirective();
    expect(directive).toBeTruthy();
  });
});
