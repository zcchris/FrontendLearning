import { PostsDirective } from './posts.directive';

describe('PostsDirective', () => {
  it('should create an instance', () => {
    const directive = new PostsDirective();
    expect(directive).toBeTruthy();
  });
});
