import { IfIsEmptyPipe } from './if-is-empty.pipe';

describe('IfIsEmptyPipe', () => {
  it('create an instance', () => {
    const pipe = new IfIsEmptyPipe();
    expect(pipe).toBeTruthy();
  });
});
